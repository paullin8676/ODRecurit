重新梳理数据库关联和修改逻辑如下：
1、每个候选人能参与多条产品线面试，每条产品线只能有一条面试记录。
2、面试记录编辑界面和查看界面只能显示当前阶段和之前阶段的字段，并且之前阶段的字段不能编辑。查看只能查看不能编辑。
3、同时只能有一条面试记录在进行中，假如要增加新的面试记录（面推）必须是符合同时只能有一条面试记录在走流程的规则限定。
4、客服负责人可以从产品线中获取，不需要增加到CandidateProductLine表中。CandidateProductLine删除字段：client_owner。
5、Interview表中的entry_date，entry_remark，leave_date，leave_reason，leave_remark删除，并且把这些字段挪到Candidate表。
6、CandidateProductLine表中的current_stage字段表示面试阶段候选人所处的阶段，字段名称改为：interview_stage。
7、Candidate表中的current_stage表示推进过程中的阶段，当进入面试阶段后，current_stage保持和最新那条面试记录的interview_stage字段保持同步。
8、韧测管理中的记录，面推规则如下（涉及最新阶段通过interview_stage判断）：
	入职或离职阶段 不能面推 
	没有可用产品线 不能面推 
	当前处于推荐面试阶段 不能面推（要等该推荐流程走完） 
	有面试记录且最新阶段是通过的 不能面推（还在面试中） 
	有面试记录且所有最新阶段都是未通过的 可以面推 
	没有面试记录（第一次面推） 可以面推


需求改动：
1、阶段配置里总共有14个阶段，从候选录入到离职，招聘管理每个模块